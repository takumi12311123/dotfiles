# Security

- Never commit secrets (.env, API keys, credentials, *.pem)
- Validate all external input (user input, API responses)
- Use parameterized queries, never string concatenation for SQL/commands
